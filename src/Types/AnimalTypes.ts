export interface AnimalTypes { 

     animalId: number,
    name: string ,
    specie: string,
    breed: string | null,
    color: string | null,
    birthDate: string | null,
    icon: string,
    gender: string | null,
    genderId: string |null
}